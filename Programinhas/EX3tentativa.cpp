#include <stdio.h>

int main(){
	int num, i, resp;
	printf("Digite um numero: ");
	scanf("%d", num);
	
	for (i = 0; i<=num; i++){
		if(i%2 !=0){
			i+= num
			printf("O quadrado de um número positivo num é pot igual à soma dos i primeiros números ímpares");
		}
		
		
		
	printf("Digite 1 para continuar, qualquer outro o programa sera encerrado.\n");
	scanf("%d", &resp);
	while(resp==1);
	
return 0;
}
